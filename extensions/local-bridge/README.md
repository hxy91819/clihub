# CLI Hub Local Bridge

Local UI-host companion extension for `MasonHuang.cli-hub`.

It registers the hidden command `clihubLocal.writeToIterm2` so CLI Hub running in a Remote SSH extension host can send path context to the user's local iTerm2 current session.

This extension has no visible commands and does not manage terminals or CLI sessions.
